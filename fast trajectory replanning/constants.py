YELLOW = (255, 255, 0) # START
PATH = (0, 255, 255) # FINAL PATH
BLUE = (0, 0, 255) #END
WHITE = (255, 255,255) #STANDARD
BLACK = (0,0,0) # BLOCKED
GREY = (127,127,127) #UNEXPANDED


ROWS = 101
NODE_LENGTH = 5

GRID_LENGTH = NODE_LENGTH*ROWS

START_NODE = (0,0)
END_NODE = (ROWS-1,ROWS-1)
GAP = NODE_LENGTH  # gap between the two panes

# Window is TWO grids side-by-side
WINDOW_W = GRID_LENGTH * 2 + GAP
WINDOW_H = GRID_LENGTH